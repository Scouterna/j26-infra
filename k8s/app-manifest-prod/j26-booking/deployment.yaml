# Self-contained prod Deployment (no dev base). DATABASE_URL is a full DSN from the
# CSI-mounted secret (prod DB DSN in Key Vault), not composed from Service Connector vars.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: j26-booking
spec:
  replicas: 1
  selector:
    matchLabels:
      app: j26-booking
  template:
    metadata:
      labels:
        app: j26-booking
    spec:
      initContainers:
        - name: j26-booking-init-migrate
          image: ghcr.io/scouterna/j26-booking-migrate:latest
          envFrom:
            - configMapRef:
                name: j26-booking-config
            - secretRef:
                name: j26-booking-secrets
          volumeMounts:
            - name: secrets-store
              mountPath: "/mnt/secrets-store"
              readOnly: true
      containers:
        - name: j26-booking-app
          image: ghcr.io/scouterna/j26-booking:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: j26-booking-config
            - secretRef:
                name: j26-booking-secrets
          volumeMounts:
            - name: secrets-store
              mountPath: "/mnt/secrets-store"
              readOnly: true
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: j26-booking-kv-csi
