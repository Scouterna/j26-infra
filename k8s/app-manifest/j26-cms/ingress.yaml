apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: j26-cms
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /_services/cms/$2
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  ingressClassName: nginx
  rules:
    - host: app.dev.j26.se
      http:
        paths:
          - path: /_services/cms(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: j26-cms
                port:
                  number: 80
