apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - secret-provider-class.yaml

configMapGenerator:
  - name: j26-scoutin-2-config-files
    files:
      - config/stepConfig.yml
      - config/stepConfig.pre-checkin.yml
      - config/dataSourceConfig.yml
