apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - secret-provider-class.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  managed-by: kustomize
